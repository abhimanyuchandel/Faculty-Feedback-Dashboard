export default function DigestUnsubscribePreviewPage() {
  return (
    <main className="page">
      <section className="container" style={{ maxWidth: "720px" }}>
        <div className="card">
          <h1>Preview Link</h1>
          <p>
            This is only the admin digest preview. Real unsubscribe links in delivered digest emails include a
            one-time token and will take faculty to the correct confirmation page.
          </p>
        </div>
      </section>
    </main>
  );
}
